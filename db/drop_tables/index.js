const dropAttributes = 'DROP TABLE IF EXISTS attribute';

const dropAttributeValue = 'DROP TABLE IF EXISTS attribute_value';

const dropAudit = 'DROP TABLE IF EXISTS audit';

const dropCart = 'DROP TABLE IF EXISTS cart';

const dropCategory = 'DROP TABLE IF EXISTS category';

const dropCustomer = 'DROP TABLE IF EXISTS customer';

const dropDepartment = 'DROP TABLE IF EXISTS department';

const dropOrderDetail = 'DROP TABLE IF EXISTS order_detail';

const dropOrders = 'DROP TABLE IF EXISTS orders';

const dropProduct = 'DROP TABLE IF EXISTS product';

const dropProductAttribute = 'DROP TABLE IF EXISTS product_attribute';

const dropProductCategory = 'DROP TABLE IF EXISTS product_category';

const dropReview = 'DROP TABLE IF EXISTS review';

const dropShipping = 'DROP TABLE IF EXISTS shipping';

const dropShippingRegion = 'DROP TABLE IF EXISTS shipping_region';

const dropShoppingCart = 'DROP TABLE IF EXISTS shopping_cart';

const dropTax = 'DROP TABLE IF EXISTS tax';

module.exports = {
  dropAttributes,
  dropAttributeValue,
  dropAudit,
  dropCart,
  dropCategory,
  dropCustomer,
  dropDepartment,
  dropOrderDetail,
  dropOrders,
  dropProduct,
  dropProductAttribute,
  dropProductCategory,
  dropReview,
  dropShipping,
  dropShippingRegion,
  dropShoppingCart,
  dropTax,
};
